{
    "type": "object",
    "required": ["ticket_id", "user_id", "event_id", "email"],
    "properties": {
      "ticket_id": {
        "type": "string",
        "description": "Unique identifier for the ticket."
      },
      "user_id": {
        "type": "string",
        "description": "Identifier of the user who owns the ticket."
      },
      "event_id": {
        "type": "string",
        "description": "Identifier of the event this ticket is for."
      },
      "email": {
        "type": "string",
        "format": "email",
        "description": "Email address associated with the ticket."
      }
    },
    "additionalProperties": true
  }
  